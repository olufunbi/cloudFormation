version: 2.1
jobs:
  fail_job:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run:
          name: Fail Job
          command: return 1
      - run:
          name: Output file
          command: echo "The build failed"
          when: on_fail               

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      - fail_job
